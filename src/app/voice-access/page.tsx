export default function VoiceAccessPage() {
  return (
    <main className="mx-auto max-w-md p-8">
      <h1 className="text-xl font-semibold">Preview access renewed</h1>
      <p className="mt-4">
        You can return to your canvas. This window closes automatically when
        voice access is ready. Your canvas and unsaved conversation remain in
        the original tab.
      </p>
    </main>
  );
}
