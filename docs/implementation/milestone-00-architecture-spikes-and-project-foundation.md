# Milestone 0 — Architecture spikes and project foundation

Status: Closed
Master plan: [`thinking-canvas-implementation-plan.md`](../../thinking-canvas-implementation-plan.md)
Plan owner: Product owner
Last updated: 2026-08-11

## Goal and user-visible outcome

Establish a deployable, authenticated Thinking Canvas foundation and resolve the expensive technical uncertainties before feature work begins.

When this milestone is complete, a reviewer can open an authenticated Netlify preview, enter a protected spike workspace, and inspect recorded demonstrations showing that collaboration, persistence, compaction, canvas interaction, rich documents, bounded AI commands, live AI voice, and reversible AI changes are viable within the approved architecture. The spike UI is evidence tooling, not the Milestone 1 product experience.

## Requirements covered

Milestone 0 does not complete any `FR-###` product requirement. It validates the architecture required by later requirements and covers these exact master-plan commitments:

- **Required voice clarification:** approve the OpenAI Realtime API alongside the Responses API; keep the OpenAI API key server-side; mint short-lived Realtime client credentials from an authenticated server route.
- **Library boundaries:** keep the canonical schema independent of Konva serialization; use Yjs as the collaboration engine with durable Supabase updates and snapshots; use Presence for participant state and Broadcast for high-frequency cursor and document-update messages; keep durable state out of Zustand; keep Lucide in application chrome; do not add a second hosted collaboration, authentication, database, or AI service without approval.
- **Initial database checklist:** create the initial relational schema, ownership rules, indexes, Row Level Security, and automated role-policy evidence described in the master plan.
- **Milestone 0 — Build checklist:** complete all nine foundation items in the master plan.
- **Milestone 0 — Required technical spikes:** complete all eight spikes in the master plan.
- **Milestone 0 — Exit gate:** “Record spike results and approve the final collaboration, persistence, rich-text, AI, and voice architecture before feature milestones begin.”

Evidence produced here informs, but does not check, `FR-007`, `FR-008` through `FR-014`, `FR-031` through `FR-035`, and `FR-044` through `FR-053`.

## Decisions required

| Decision                              | Owner         | Options and consequences                                                                                                                                                                                                                                                     | Required timing                      | Status                                                                                                                                                                                         |
| ------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Realtime voice architecture           | Product owner | Approve OpenAI Realtime alongside Responses and retain live conversation requirements; or revise `FR-008` through `FR-014` and `FR-060` to turn-based voice before development.                                                                                              | Before the voice-spike slice begins. | Approved 2026-08-10: use OpenAI Realtime alongside Responses and retain the live-conversation requirements.                                                                                    |
| Spike performance target              | Product owner | Approve the proposed baseline of a current Mac used by the product owner, current Chrome at a desktop viewport, 1,000 visible mixed objects, responsive direct manipulation, and no sustained interaction below 30 fps; or provide different target hardware and thresholds. | Before canvas-spike acceptance.      | Approved 2026-08-10: use the proposed spike baseline. This does not close the later production budget decision `PD-007`.                                                                       |
| GitHub branch protection availability | Product owner | Make the repository public so the existing master-plan requirement can be satisfied on the current GitHub plan.                                                                                                                                                              | Before Slice 1 can complete.         | Resolved 2026-08-10: repository visibility is `PUBLIC`; `main` protection now enforces pull requests, strict required `quality` checks, stale-review dismissal, and administrator enforcement. |
| Final architecture                    | Product owner | Approve the validated collaboration, persistence, rich-text, AI, and voice choices; require a bounded follow-up experiment; or revise the master-plan architecture before feature work.                                                                                      | At the Milestone 0 exit gate.        | Open; closure decision, separate from plan approval.                                                                                                                                           |

`PD-001` through `PD-006`, `PD-008`, and `PD-010` remain deferred to their owning feature milestones. `PD-009` does not block this milestone because the spike covers temporary disconnect recovery, not deliberate offline editing. The Milestone 0 measurements will inform the final `PD-007` production budget; they do not close it.

## Technical approach

### Application foundation

- Scaffold a strict TypeScript Next.js App Router application with Tailwind CSS and shadcn/ui.
- Keep browser, server, and shared modules explicit. Server-only Supabase and OpenAI clients must not be importable into client bundles.
- Use Zod-versioned domain schemas and a renderer-independent object model. Konva nodes are derived views and never persistence payloads.
- Use Zustand only for ephemeral UI state such as active tool, selection, viewport mode, and open panels.
- Expose one `executeCommand` boundary for human and AI mutations. Each command carries actor, origin, target canvas, validated payload, permission result, undo data, audit metadata, and collaboration update metadata.

### Collaboration and persistence

- Keep shared canvas content in a Yjs document. Use Supabase Realtime Broadcast to exchange Yjs updates and Presence for slow-changing participant state.
- Persist append-only updates with monotonic server-assigned sequence metadata. Load the latest compacted snapshot, subscribe with an overlap-safe sequence boundary, fetch subsequent updates, and rely on Yjs idempotency for repeated delivery.
- Compact in a trusted server process: lock one canvas compaction, merge the selected update range into a snapshot, verify the reconstructed state, publish the snapshot transactionally, and prune only the covered updates after verification.
- Treat disconnect recovery as a required spike. Deliberate offline product behavior remains a later decision.

### Spike harness

- Build authenticated, development/preview-only routes that exercise real domain boundaries and expose deterministic controls, metrics, and downloadable privacy-safe evidence.
- Keep spike fixtures reproducible and non-sensitive. Spike-only UI must be clearly marked and removable without changing canonical domain modules.
- Implement the smallest mixed-object model needed to test pan, zoom, selection, transforms, connectors, and 1,000 visible objects; defer polished creation tools and full object/action coverage to Milestone 1.
- Mount a Lexical editor only while a document is focused. Bind its state to a document-owned Yjs substructure and prove that internal visual-object IDs and coordinate space remain isolated from the parent canvas.

### AI, voice, and reversal

- Build an authenticated server route that accepts a bounded semantic canvas projection, calls the Responses API, validates returned tool arguments, and passes allowed results through the same command boundary used by humans. OpenAI receives no database credentials or direct mutation authority.
- Build a separate authenticated route that verifies membership and returns a short-lived Realtime client credential. The browser connects to OpenAI Realtime over WebRTC; the long-lived key remains server-only.
- Model an AI change as per-object before and after state plus stable object identity and affected-field metadata. Reversal restores only the AI-owned fields when they remain applicable, preserving unrelated later edits; conflicts must produce an explicit non-destructive result.

### Failure and deployment behavior

- Distinguish local, preview, and production configuration. Fail startup or the affected route with a clear non-secret error when required configuration is missing.
- Surface collaboration connection, synchronization, persistence, compaction, AI validation, voice credential, and reversal failures in the spike harness and structured privacy-safe server logs.
- Deploy each reviewable slice to Netlify preview. Production configuration is established but no public product launch occurs in this milestone.

## Database and security changes

Commit forward-safe Supabase migrations for the master plan's initial schema:

- Identity and access: `profiles`, `canvases`, `canvas_members`, `canvas_invitations`.
- Collaboration durability: `canvas_updates`, `canvas_snapshots`, including monotonic sequence/version metadata and compaction coverage.
- Feedback: `comments`, `comment_targets`, `comment_replies`, `comment_prompts`, `comment_responses`.
- AI review: `ai_change_sets`, `ai_object_changes`, `review_decisions`, including reversible before/after payloads.
- Stories: `stories`, ordered `story_scenes`.
- Reuse: `starter_templates`.

For every table:

- Add primary and foreign keys, ownership constraints, timestamps, and indexes for expected membership and canvas-scoped access paths.
- Enable Row Level Security before application access is granted.
- Define owner, editor, commenter, and viewer policies from an explicit permission matrix. Service-role use is limited to trusted server maintenance that cannot safely run as the user.
- Reject unauthenticated reads and writes. Re-check current membership for AI reads, AI commands, and Realtime credential issuance.
- Seed only deterministic local fixtures with synthetic identities and content; preview fixtures use non-production data.
- Add automated policy tests that prove both allowed and denied operations for every role and table.

Migrations are additive during this milestone. Any destructive correction requires a reviewed compensating migration and a local database reset rehearsal; production data migration is out of scope.

## Ordered task checklist

### Slice 1 — Repository and deployable shell

- [x] Resolve the GitHub plan/visibility decision, then configure `main` protection with pull-request and required-test rules as required by the master plan.
- [x] Scaffold the strict Next.js App Router project, Tailwind CSS, and shadcn/ui without overwriting the master plan.
- [x] Pin dependency versions, review licenses, and commit the lockfile.
- [x] Add formatting, linting, type-checking, Vitest, Testing Library, Playwright, and axe commands.
- [x] Add CI for formatting, lint, type, unit, end-to-end, and accessibility checks. Integration and migration checks will join this gate when their Slice 2 and Slice 3 suites exist.
- [x] Add validated environment configuration and safe example files for local, Netlify preview, and production scopes.
- [x] Connect Netlify and prove distinct preview and production deployment contexts.

### Slice 2 — Authentication, schema, and command boundary

- [x] Configure Supabase local development and server-readable Auth sessions.
- [x] Add protected application and spike routes with sign-in, sign-out, and expired-session behavior.
- [x] Commit the initial database migrations, indexes, constraints, RLS policies, and synthetic local seed fixtures.
- [x] Add role-policy integration tests for owner, editor, commenter, viewer, non-member, and unauthenticated identities.
- [x] Define versioned Zod schemas for shared objects and server payloads.
- [x] Implement and test the shared human/AI command boundary, including permission denial, undo metadata, audit metadata, and collaboration updates.

### Slice 3 — Collaboration, persistence, and compaction

- [x] Implement a renderer-independent Yjs canvas document and deterministic state hashing for evidence.
- [x] Connect authorized Supabase Broadcast and Presence channels.
- [x] Implement overlap-safe snapshot-plus-update loading and append-only persistence.
- [x] Implement trusted compaction with locking, state verification, transactional snapshot publication, and covered-update pruning.
- [x] Automate reordered, repeated, disconnected, reconnect, load-race, and compaction equivalence cases.
- [x] Run the two-browser collaboration, persistence, and compaction spike scenarios on Netlify preview.

### Slice 4 — Canvas and rich-document feasibility

- [x] Implement the minimal Konva spike surface with pan, pointer-centered zoom, select, move, resize, and connector anchoring.
- [x] Generate a deterministic 1,000-object mixed fixture and record frame-time and interaction measurements on the approved target hardware.
- [x] Implement a focused Lexical document with Yjs binding and isolated document-internal visual objects.
- [x] Prove focus entry/exit, parent-canvas isolation, update convergence, and reload for the rich-document spike.

### Slice 5 — AI, voice, and reversal feasibility

- [x] Implement the bounded canvas projection and authenticated Responses API spike route.
- [x] Validate AI tool arguments and execute accepted AI mutations only through the shared command boundary.
- [x] Add malformed, unauthorized, oversized, and prompt-injection fixture cases.
- [x] Implement authenticated short-lived Realtime credential issuance and browser WebRTC connection.
- [x] Prove the live voice connection on a Netlify preview without exposing the long-lived API key.
- [x] Implement per-object AI before/after records and field-aware reversal.
- [x] Prove immediate restoration while preserving a later unrelated human edit.

### Slice 6 — Architecture decision and milestone evidence

- [x] Consolidate commands, logs, screenshots, metrics, preview deploy identifiers, and limitations in this document.
- [x] Trace evidence to every build-checklist item, technical spike, library boundary, initial database item, and exit-gate clause.
- [x] Record proposed final collaboration, persistence, rich-text, AI, and voice architecture, including any approved deviations.
- [x] Run the complete affected suite and authenticated Netlify preview walkthrough.
- [x] Request product-owner closure approval; do not begin Milestone 1 until it is granted.

## Pull-request slices

Pull requests are proposed for reviewability but are not authorized by plan approval alone. Open them only when the product owner explicitly requests or approves PR creation.

| Slice                                 | Depends on | Demoable outcome                                                                                          | Tests                                                                                                           | Rollback or compensating path                                                                          |
| ------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1. Project shell and delivery         | None       | A typed shell passes CI and deploys distinct Netlify contexts.                                            | Format, lint, type, unit smoke, Playwright smoke, axe smoke, deployment smoke.                                  | Revert scaffold/config commits; no persistent data.                                                    |
| 2. Auth, schema, command core         | Slice 1    | An authenticated user reaches a protected spike workspace; role tests and commands enforce permissions.   | Auth integration, migration/reset, complete RLS matrix, schema and command unit tests.                          | Additive compensating migration and route/config revert.                                               |
| 3. Collaboration durability           | Slice 2    | Two browsers converge, reload from durable state, and survive verified compaction.                        | Yjs update permutation tests, reconnect/load-race integration, two-browser E2E, snapshot equivalence.           | Disable compaction/pruning; replay append-only updates from last verified snapshot.                    |
| 4. Canvas and document spikes         | Slice 3    | The 1,000-object canvas remains usable and a focused collaborative document stays isolated.               | Geometry/connector tests, performance capture, Lexical isolation/convergence tests, preview walkthrough.        | Remove spike routes while retaining validated domain primitives.                                       |
| 5. AI, voice, and reversal spikes     | Slices 2–3 | Validated AI commands, live preview voice, and non-destructive reversal work through approved boundaries. | Route authorization/schema tests, adversarial fixtures, WebRTC preview evidence, concurrent-edit reversal test. | Disable AI/voice routes by environment flag; preserve audit data; no direct database authority exists. |
| 6. Evidence and architecture approval | Slices 1–5 | One traceable record supports an informed architecture decision.                                          | Full affected suite and final preview scenario.                                                                 | Keep milestone open and run a bounded follow-up spike.                                                 |

## Automated and manual tests

Exact package-script names will be recorded after scaffold approval. The planned verification surface is:

| Area          | Automated verification                                                                                                  | Manual/preview scenario                                                                                                | Expected result and retained evidence                                                                                              |
| ------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Quality       | Format, lint, strict type check, unit, integration, Playwright, axe, migration tests.                                   | Open the authenticated shell in current Chrome and Safari.                                                             | All commands exit zero; CI run and deploy identifiers recorded.                                                                    |
| Auth/RLS      | Policy matrix for every table and role, including non-member and unauthenticated denial.                                | Sign in, sign out, expire session, and attempt a protected route.                                                      | Only permitted rows/actions succeed; test report and privacy-safe denial logs retained.                                            |
| Collaboration | Permute, duplicate, delay, and reconnect Yjs updates; compare deterministic state hashes.                               | Two authenticated browser contexts edit simultaneously, disconnect one, continue editing, reconnect, and reload both.  | Final state hashes match with zero lost committed edits; video or screenshots plus logs retained.                                  |
| Persistence   | Snapshot/update overlap and connection-race integration tests.                                                          | Open a fresh third client during active edits.                                                                         | It loads the latest snapshot plus every subsequent update without a gap; sequence trace retained.                                  |
| Compaction    | Reconstruct before/after compaction and compare state vectors/hashes; retry the job.                                    | Trigger preview compaction, then reload a fresh client.                                                                | State is unchanged, retry is idempotent, and only covered updates are pruned; metrics retained.                                    |
| Canvas        | Geometry, transform, connector-anchor, culling, and fixture tests.                                                      | Pan, zoom, select, move, and resize on the approved target with 1,000 visible mixed objects.                           | Connectors remain attached and interaction meets the approved spike threshold; trace and hardware/browser details retained.        |
| Rich document | Lexical/Yjs convergence and parent/child ownership tests.                                                               | Focus and edit a document while another browser updates it; add/move an internal object; exit focus and reload.        | Text converges, internal objects remain isolated, and content reloads; screenshots and hashes retained.                            |
| AI            | Projection bound, Zod tool validation, authorization, malformed call, oversized context, and prompt-injection fixtures. | Ask AI for one permitted spike mutation and one disallowed mutation.                                                   | Only validated, authorized commands execute and OpenAI has no database authority; request ID and redacted command record retained. |
| Voice         | Credential-route auth/expiry tests and browser secret-bundle scan.                                                      | From authenticated Netlify preview, obtain an ephemeral credential and establish, end, and reconnect a WebRTC session. | Live session connects without the long-lived key appearing in browser or logs; deploy ID and connection metrics retained.          |
| Reversal      | Field-aware before/after and conflict tests.                                                                            | Apply an AI label change, make an unrelated human position change, then discard the AI change.                         | Prior label is restored immediately while the later position remains; state diff retained.                                         |

The final preview walkthrough uses non-production identities and data. No test may be described as passing until its command or scenario has actually run.

## Risks and assumptions

| Risk or assumption                                                                      | Likelihood / impact | Mitigation or experiment                                                                                                                                                     | Owner                         | Status                                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Supabase Broadcast can reorder or duplicate messages and is not durable storage.        | Expected / high     | Use Yjs idempotency plus durable sequenced updates and automate permutation/reconnect cases.                                                                                 | Engineering                   | Planned.                                                                                                                                                                                                    |
| A snapshot/subscription race could lose edits.                                          | Medium / critical   | Subscribe with an overlap-safe boundary, fetch subsequent sequences, deduplicate, and test a new client during active editing.                                               | Engineering                   | Planned.                                                                                                                                                                                                    |
| Unsafe compaction could prune needed updates.                                           | Medium / critical   | Lock per canvas, verify reconstructed state, publish transactionally, retain last verified snapshot, and prune only its covered range.                                       | Engineering                   | Planned.                                                                                                                                                                                                    |
| Konva with 1,000 visible mixed objects may miss the approved interaction budget.        | Medium / high       | Measure early, separate static/interactive layers, limit redraws, and use `rbush` for viewport/selection queries.                                                            | Engineering and product owner | Spike target approved 2026-08-10.                                                                                                                                                                           |
| Lexical focus and Konva transforms may compete for keyboard/pointer ownership.          | Medium / medium     | Use an explicit focus state and input-routing boundary; test entry, editing, exit, and collaboration.                                                                        | Engineering                   | Planned.                                                                                                                                                                                                    |
| AI output could bypass permissions or corrupt shared state.                             | Medium / critical   | No direct DB authority; validate Zod tools and re-check membership through the shared command boundary.                                                                      | Engineering                   | Planned.                                                                                                                                                                                                    |
| Field-aware reversal may encounter a later human edit to the same field.                | Medium / high       | Never overwrite silently; prove unrelated-field preservation and return an explicit conflict for same-field divergence.                                                      | Product owner and engineering | Conflict behavior must be validated at final architecture review.                                                                                                                                           |
| Netlify preview, Supabase, or OpenAI credentials/projects may not yet exist.            | Medium / high       | Inventory configuration before each dependent slice; create only approved non-production resources and keep secrets out of Git/evidence.                                     | Product owner and engineering | Resolved for Milestone 0: Netlify and Supabase preview are verified, and a product-owner OpenAI project key now supports hosted Responses plus an authenticated ephemeral-credential Realtime conversation. |
| Local Docker Desktop may fail to start or mount a complete Supabase tool container set. | Confirmed / medium  | Use a clean synthetic reset and only the services required by the active slice; retain the standard Supabase commands for Linux CI and a direct local PostgreSQL pgTAP path. | Engineering                   | Mitigated: database, Kong, Auth, PostgREST, and mail services start; `pnpm db:reset` and Auth E2E pass. The local `pg_prove` wrapper still cannot mount this workspace path, while direct pgTAP passes.     |
| The repository was private on a GitHub plan that did not expose branch protection.      | Confirmed / high    | Product owner selected public visibility; protect `main` with pull requests, strict required checks, and administrator enforcement.                                          | Product owner and engineering | Resolved 2026-08-10; GitHub readback confirms the required `quality` check and protected `main`.                                                                                                            |
| The master implementation plan itself is marked `Draft for review`.                     | High / high         | Milestone 0 and its listed decisions are approved, but any architecture change outside that documented scope still requires an explicit master-plan revision and approval.   | Product owner                 | Milestone 0 scope approved 2026-08-10; broader ledger approval remains separate.                                                                                                                            |

## Proposed final architecture

Status: Approved by the product owner on 2026-08-11.

| Boundary                           | Proposed final choice                                                                                                                                                                                                                                                                                                                                                                 | Spike evidence and production constraint                                                                                                                                                                                                                                                                                      |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Collaboration                      | Keep canonical shared content in renderer-independent Yjs documents. Exchange high-frequency Yjs updates over private, membership-authorized Supabase Broadcast channels and slow-changing participant state through Supabase Presence.                                                                                                                                               | Reordered, repeated, simultaneous, disconnected, and reconnected updates converged in unit, local two-browser, and authenticated hosted scenarios. Presence was not persisted as canvas history.                                                                                                                              |
| Persistence and compaction         | Persist server-sequenced append-only Yjs updates and verified snapshots in Supabase PostgreSQL. Load by subscribing before overlap-fetching snapshot-plus-updates. Compact under a per-canvas advisory lock, publish only after digest equivalence, and prune only the verified covered range.                                                                                        | Load-race, duplicate delivery, fresh-client restore, compaction equivalence, immediate retry, and covered-range pruning passed locally, in CI, and in the hosted walkthrough. Deliberate offline editing remains `PD-009`, outside Milestone 0.                                                                               |
| Canvas rendering                   | Keep canonical object geometry independent of Konva and use Konva only as the derived renderer and interaction layer. Apply transient camera transforms imperatively so panning and zooming do not reconcile all visible nodes on every frame.                                                                                                                                        | The hosted 1,000-object scenario averaged 72.7 fps with an 18.1 ms p95 and no sustained period below 30 fps; the product-owner run averaged 74.1 fps. Production culling and spatial indexing remain later milestone work.                                                                                                    |
| Rich text and document containment | Mount Lexical only in explicit focused-document interaction. Store document text and visual objects in a document-owned Yjs substructure with a separate coordinate and ownership boundary that excludes parent-canvas connector fields. Use the proven explicit Lexical-to-`Y.Text` bridge as the current baseline.                                                                  | Two tabs converged and reload restored plain text plus an isolated internal visual. The packaged Lexical collaboration plugin did not update the supplied Yjs document reliably in the spike lifecycle, so rich formatting and the production provider integration remain Milestone 7 work.                                   |
| AI reasoning and mutation          | Use the OpenAI Responses API only from authenticated server routes. Send a deterministic bounded semantic projection, treat canvas content as untrusted data, require strict tool calls, reparse arguments with Zod, re-check target scope and current permission, and execute only through `executeCommand`. OpenAI receives no Supabase credential or direct persistence authority. | Oversized, malformed, unauthorized, and prompt-injection fixtures failed before mutation; the hosted permitted request produced a validated command and privacy-safe provider request evidence. Full-canvas relevance policy, budgets, cancellation, and product UI remain Milestone 4 work.                                  |
| Live AI voice                      | Keep the standard OpenAI key in Netlify Functions only. An authenticated route re-checks current canvas membership and mints a short-lived Realtime client credential; the browser connects directly to OpenAI over WebRTC after explicit microphone consent and tears down tracks, data channel, and peer connection on disconnect.                                                  | The product owner completed and ended a two-way hosted Realtime conversation without the standard key entering the browser, logs, fixtures, or Git. Human-to-human voice transport, consent/retention policy, captions, reconnection UX, and interruption behavior remain `PD-001`, `PD-005`, `PD-008`, and Milestone 9 work. |
| AI reversal                        | Retain stable object identity, complete before/after images, and affected-field paths. Restore an AI-owned field only when its current value still matches the AI after-image; otherwise return an explicit non-destructive conflict.                                                                                                                                                 | Reversal restored AI text while preserving a later human position edit; a later edit to the same text field produced a conflict instead of silent overwrite.                                                                                                                                                                  |

### Approved implementation deviations retained in the proposal

- Netlify uses the explicit `@netlify/plugin-nextjs` adapter because the first raw `.next` artifact reported ready but returned 404.
- The canvas spike uses imperative Konva camera transforms because React state updates on every animation frame averaged only 16.5 fps with the same 1,000-object fixture.
- The rich-document baseline uses an explicit Lexical-to-`Y.Text` bridge because the packaged collaboration plugin did not update the supplied Yjs document in the spike lifecycle.
- macOS local verification uses direct PostgreSQL pgTAP execution when the Supabase test wrapper cannot mount the workspace path; Linux CI remains the canonical clean-start database and RLS gate.
- Hosted multi-tab evidence used two independent Realtime clients under the approved synthetic owner session because Netlify team protection denied a browser profile that was not a Netlify project member. Local E2E separately proves distinct owner/editor roles.

## Master-plan traceability

The master-plan boxes remain unchanged until explicit closure approval. “Pass” here means the milestone record contains implementation and evidence for the cited item; it is not the closure decision.

### Required voice clarification and library boundaries

| Master-plan item                                                                                                                                     | Evidence                                                                                                                                 | Trace status |
| ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Keep the OpenAI API key server-side and mint short-lived Realtime client credentials from an authenticated server route.                             | Realtime route authorization/expiry unit tests; authenticated hosted conversation; Netlify secret metadata and final deploy secret scan. | Pass         |
| Treat Konva as a renderer and interaction layer; keep the canonical canvas schema independent of Konva node serialization.                           | `src/domain/canvas-object.ts`, geometry tests, Slice 4 hosted canvas walkthrough, and the 1,000-object benchmark.                        | Pass         |
| Treat Yjs as the collaboration state engine; persist compacted updates and snapshots in Supabase instead of relying on transient Broadcast delivery. | Slice 3 unit/integration/E2E evidence, `canvas_updates`, `canvas_snapshots`, and hosted compaction/reload walkthrough.                   | Pass         |
| Use Supabase Presence for slow-changing participant state and Broadcast for high-frequency cursor and document-update messages.                      | Private channel implementation and two-client hosted Presence/Broadcast walkthrough.                                                     | Pass         |
| Keep Zustand state local only; durable or collaborative product state must not live exclusively in a Zustand store.                                  | Repository inspection finds no durable Zustand-backed state; durable data is in Yjs and PostgreSQL.                                      | Pass         |
| Keep Lucide icons in the application chrome. A searchable icon library for insertion onto the canvas remains deferred.                               | Repository inspection and rendered spike UI show Lucide only in application controls; no insertable icon library exists.                 | Pass         |
| Do not add a second hosted collaboration, authentication, database, or AI service without an approved architecture change.                           | Runtime inventory remains Netlify, Supabase, and OpenAI only.                                                                            | Pass         |

### Initial database checklist

| Master-plan item                                                                                                                  | Evidence                                                                                                                                                             | Trace status |
| --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Create migrations for `profiles`, `canvases`, `canvas_members`, and `canvas_invitations`.                                         | Initial schema migration, clean reset, generated types, and role-policy tests.                                                                                       | Pass         |
| Create migrations for `canvas_updates` and `canvas_snapshots`, including monotonic sequence/version metadata.                     | Initial and collaboration-durability migrations; append/load/compaction tests.                                                                                       | Pass         |
| Create migrations for `comments`, `comment_targets`, `comment_replies`, `comment_prompts`, and `comment_responses`.               | Initial schema migration and complete RLS matrix.                                                                                                                    | Pass         |
| Create migrations for `ai_change_sets`, `ai_object_changes`, and `review_decisions` with reversible before/after payloads.        | Initial schema migration, complete RLS matrix, and reversal-domain tests.                                                                                            | Pass         |
| Create migrations for `stories` and ordered `story_scenes`.                                                                       | Initial schema migration, order constraint/indexes, and complete RLS matrix.                                                                                         | Pass         |
| Create migrations for `starter_templates`.                                                                                        | Initial schema migration and complete RLS matrix.                                                                                                                    | Pass         |
| Add foreign keys, ownership rules, timestamps, and indexes for all access paths.                                                  | Clean migration replay, schema inspection, and 105 database assertions in final Slice 5 CI.                                                                          | Pass         |
| Enable Row Level Security on every user-owned table before application access is enabled.                                         | Migration enables RLS on all 17 user-owned tables before grants; policy matrix passes.                                                                               | Pass         |
| Prove with automated policy tests that owners, editors, commenters, and viewers receive only their permitted data and operations. | 96 role-policy assertions plus nine collaboration durability assertions cover allowed and denied operations, non-members, former members, and unauthenticated users. | Pass         |

### Milestone 0 build checklist

| Master-plan item                                                                                                                                    | Evidence                                                                                                                     | Trace status |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Create the GitHub repository with `main` protected by pull-request and required-test rules.                                                         | Public repository readback: strict required `quality`, pull requests, stale-review dismissal, and administrator enforcement. | Pass         |
| Scaffold a strict TypeScript Next.js App Router project with Tailwind CSS and shadcn/ui.                                                            | Slice 1 commits, pinned lockfile, strict type check, build, and ready Netlify artifacts.                                     | Pass         |
| Add formatting, linting, type-checking, unit-test, and end-to-end-test commands.                                                                    | `package.json`, local checks, and protected CI runs.                                                                         | Pass         |
| Connect Netlify to GitHub and produce working preview and production deployment contexts.                                                           | Ready production, branch, and immutable deploy-preview identifiers recorded in the evidence table.                           | Pass         |
| Configure separate local, preview, and production environment variables without committing secrets.                                                 | Zod environment boundaries, safe example files, Netlify context metadata, `.gitignore`, and secret scans.                    | Pass         |
| Integrate Supabase Auth using server-readable sessions and protected application routes.                                                            | Auth route implementation and Chromium sign-in/sign-out/expired-session scenarios locally and on preview.                    | Pass         |
| Commit Supabase migrations and seed only non-sensitive local development fixtures.                                                                  | Two forward-safe migrations, deterministic synthetic seed, clean reset, and preview-project migration.                       | Pass         |
| Define versioned Zod schemas for every shared object and server API payload.                                                                        | Versioned object, command, AI projection/tool, Realtime, and persistence schemas with unit and route tests.                  | Pass         |
| Define one command boundary for all human and AI mutations so permissions, undo data, audit metadata, and collaboration updates cannot be bypassed. | `executeCommand` permission matrix and metadata tests; hosted AI command passes through the same boundary.                   | Pass         |

### Required technical spikes

| Master-plan spike                                                                                                                  | Evidence                                                                                                      | Trace status |
| ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ------------ |
| Two-browser collaboration convergence after reordered, repeated, and temporarily disconnected Yjs updates over Supabase Broadcast. | Slice 3 permutations, four-browser-test suite, and authenticated two-client walkthrough with matching hashes. | Pass         |
| New-client persistence from latest snapshot plus subsequent updates without missing edits made during connection.                  | Overlap loader/race tests and fresh-viewer hosted reload after active edits.                                  | Pass         |
| Safe compaction, equivalent state, and covered-update pruning.                                                                     | Digest equivalence, transaction/lock tests, hosted three-update prune, fresh reload, and zero-prune retry.    | Pass         |
| Canvas pan, zoom, select, move, resize, connector anchoring, and 1,000 visible mixed objects on target hardware.                   | Geometry/unit tests, product-owner 74.1 fps run, and hosted 72.7 fps walkthrough.                             | Pass         |
| Focused Lexical editor with isolated document-internal visual objects.                                                             | Two-tab convergence, focus entry/exit, isolated internal visual, and reload evidence.                         | Pass         |
| Bounded Responses projection and validated domain commands with no direct database authority.                                      | Projection/tool/adversarial tests and authenticated hosted Responses mutation.                                | Pass         |
| Authenticated browser-to-OpenAI WebRTC through an ephemeral credential on Netlify preview.                                         | Credential-route and WebRTC tests plus product-owner two-way hosted conversation and clean disconnect.        | Pass         |
| AI before image, after image, and immediate restoration despite a later unrelated edit.                                            | Reversal unit/conflict tests and hosted text restoration preserving position `620, 280`.                      | Pass         |

### Exit-gate clauses

| Exit criterion                                                                                           | Evidence                                                                                                                                            | Trace status |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Every build-checklist item is implemented and linked.                                                    | Build-checklist trace table above.                                                                                                                  | Pass         |
| Every spike records setup, fixture, scenario, result, measurements, limitations, and preview identifier. | Implementation record, verification table, risk table, and final architecture proposal.                                                             | Pass         |
| Collaboration convergence, fresh-client load, and retry-safe compaction.                                 | Slice 3 local, CI, and hosted evidence summarized above.                                                                                            | Pass         |
| Canvas interactions and 1,000-object performance meet the approved target.                               | Slice 4 product-owner and hosted metrics.                                                                                                           | Pass         |
| Focused collaborative Lexical document keeps internal visuals isolated.                                  | Slice 4 two-tab and reload evidence.                                                                                                                | Pass         |
| Responses receives a bounded projection and returns validated commands without database authority.       | Slice 5 adversarial and hosted Responses evidence.                                                                                                  | Pass         |
| Authenticated Realtime WebRTC uses an ephemeral credential and exposes no long-lived key.                | Slice 5 unit, hosted conversation, secret metadata, and deploy secret scan.                                                                         | Pass         |
| AI discard restores the prior field without reverting unrelated later work.                              | Slice 5 unit and hosted reversal evidence.                                                                                                          | Pass         |
| RLS policy tests cover every documented role and denial case.                                            | 105 database assertions in protected CI.                                                                                                            | Pass         |
| Complete authenticated Netlify preview walkthrough.                                                      | Slice 6 authenticated walkthrough on the protected Slice 5 application artifact passed collaboration, canvas, document, AI, and reversal scenarios. | Pass         |
| Product owner approves the final architecture and exact master-plan exit gate.                           | Approved by the product owner on 2026-08-11.                                                                                                        | Pass         |

## Exit criteria

- [x] Every Milestone 0 build-checklist item is implemented and linked to evidence.
- [x] Every required technical spike records setup, fixture, command or scenario, expected result, actual result, measurements, limitations, and preview deploy identifier.
- [x] Collaboration converges after reordered, repeated, simultaneous, and temporarily disconnected updates in the documented two-browser preview scenario.
- [x] A fresh client loads a snapshot plus subsequent updates without losing edits made during connection.
- [x] Compaction produces an equivalent document, is retry-safe, and prunes only verified covered updates.
- [x] Pan, zoom, select, move, resize, connector anchoring, and 1,000 visible mixed objects meet the approved spike target on documented hardware and browser.
- [x] A collaborative Lexical document works in focused canvas interaction and keeps internal visual objects isolated.
- [x] The Responses API receives only a bounded structured projection and returns validated commands without direct database authority.
- [x] Authenticated browser-to-OpenAI WebRTC works through an ephemeral credential on Netlify preview, with no long-lived key in the browser, logs, fixtures, or Git.
- [x] Discarding an AI change restores its prior value without reverting a later unrelated human edit.
- [x] RLS policy tests prove the documented allowed and denied operations for owners, editors, commenters, viewers, non-members, and unauthenticated users.
- [x] The authenticated Netlify preview walkthrough passes, and local-only results are not represented as preview evidence.
- [x] The product owner approves the final collaboration, persistence, rich-text, AI, and voice architecture.
- [x] The exact master-plan exit gate is satisfied: “Record spike results and approve the final collaboration, persistence, rich-text, AI, and voice architecture before feature milestones begin.”

## Explicitly excluded work

- Milestone 1's complete canvas object creation/styling experience, multiplayer product UI, autosave UX, undo/redo experience, and `FR-001` through `FR-007` completion.
- The polished workspace experience and interaction system from Milestone 2.
- Production-ready comments and structured feedback from Milestone 3.
- The primary AI collaborator product experience, full semantic interpretation, typed conversation, and adjustable authority from Milestone 4.
- The full review workflow and guided review story from Milestone 5; this milestone proves only reversal feasibility.
- Production annotation, first-class document, guided story, live-conversation, and template experiences from Milestones 6 through 10.
- Human-to-human voice transport selection and implementation (`PD-001` and `FR-010`).
- Deliberate offline editing (`PD-009`), export/portability (`PD-010`), launch hardening, production data, domain configuration, and public release.
- Every item listed under **Explicitly deferred** in the master plan.

## Implementation record

Implementation authorized on 2026-08-10. Draft PR creation was authorized on 2026-08-10.

Slice 1 established Next.js 16.3.0, React 19.2.8, TypeScript 5.9.3, Tailwind CSS 4.3.3, shadcn/ui, the approved runtime libraries, and exact dependency versions in `pnpm-lock.yaml`. It added a minimal foundation shell, Zod environment boundaries, Prettier, ESLint, Vitest/Testing Library, Playwright/axe, GitHub Actions, and `netlify.toml` with distinct local, deploy-preview, branch-deploy, and production contexts. The Netlify CLI is authenticated and the local checkout is linked to the `thinking-canvas` site; the Netlify GitHub app has access only to `jewilhel/Thinking-Canvas`.

The foundation was delivered directly to `main` in commits `c8741d8`, `2c5fa4d`, and `f8c521e`; no pull request was created. The first production artifact exposed a packaging defect: Netlify uploaded raw `.next` internals without its Next.js adapter, so the provider reported a ready deploy while `/` returned 404. Commit `f8c521e` made `@netlify/plugin-nextjs` 5.15.13 explicit. The replacement production deploy and a separate deploy-preview context both rendered the expected shell in the signed-in browser. The Netlify project remains private, consistent with the Milestone 0 exclusion of public launch.

The production dependency license inventory contains MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, BlueOak-1.0.0, Python-2.0, CC-BY-4.0, and one LGPL-3.0-or-later transitive binary package (`@img/sharp-libvips-darwin-arm64`). No license was treated as final release approval; the master plan's production-release review remains required.

Slice 2 work began on `codex/milestone-0-slice-2`. It added local Supabase configuration and CI database commands; server/browser Supabase clients; cookie-refresh proxy handling; protected `/app` and `/spikes` routes; sign-in and sign-out server actions; the initial 17-table migration with indexes, constraints, triggers, grants, and RLS; deterministic synthetic seed data; generated database types; a 96-assertion role-policy matrix; strict versioned shared-object schemas; and one permission-aware human/AI command boundary that emits reversible undo, audit, and collaboration metadata.

The migration and seed were replayed through `pnpm db:reset`, and the role-policy matrix passes against the resulting database. The first real password login exposed a nullable `auth.users.email_change_token_new` seed defect; setting every required GoTrue token field to a deterministic empty string fixed the fixture. A real Chromium session now proves unauthenticated redirect, seeded password sign-in, server-rendered user identity, protected `/app` and `/spikes` access, sign-out revocation, and rejection of an expired session with an invalid refresh token.

Slice 2's six implementation items are complete. The free `thinking-canvas-preview` Supabase project in `us-west-1` is linked to the CLI; its migration and synthetic preview seed were applied without production data. Netlify deploy-preview and branch-deploy contexts contain only the remote project's public URL and publishable key. Production configuration was not changed.

PR [#1](https://github.com/jewilhel/Thinking-Canvas/pull/1) was squash-merged to protected `main` as `5d6d9e6`. GitHub Actions run `31464474986` passed the complete post-merge `quality` job. Netlify production deploy `6a7abe34ae9b9300085228b6` built the same merge commit; production Auth configuration remained intentionally separate and unchanged. The signed-in deploy-preview browser evidence remains the Slice 2 Auth acceptance surface. The milestone-wide authenticated preview exit criterion remains open because later collaboration, AI, voice, and reversal scenarios do not exist yet.

Slice 3 began on `codex/milestone-0-slice-3`. It adds a renderer-independent Yjs object document and SHA-256 state evidence; private, membership-authorized Supabase Broadcast and Presence; server-assigned append sequences; subscribe-before-fetch overlap loading with compaction-race retry; and an authenticated spike panel for concurrent mutation, disconnect, reconnect, reload, presence, hashing, and compaction. Browser writes cannot choose sequence or snapshot metadata. The trusted Next.js compaction route reconstructs Yjs state, verifies its digest, and calls one advisory-locked database transaction that publishes the snapshot and prunes only its covered update range. Repeating the same compaction returns the existing verified snapshot without pruning again.

Local Slice 3 verification completed the first five checklist items. The additive migration replayed from a clean reset. Direct local PostgreSQL execution passed the existing 96-assertion role matrix plus nine collaboration durability assertions; the Supabase test wrapper remains blocked only by the documented macOS Docker mount limitation. Twenty-four unit tests cover reordered and repeated Yjs updates, load overlap, compaction races, and snapshot equivalence. The four-test Chromium suite adds a real two-browser owner/editor session with simultaneous changes, temporary editor disconnect, durable catch-up, fresh-viewer snapshot reload, private Presence, verified pruning, and idempotent recompaction.

Commits `33d219d` and `1065cba` publish Slice 3 on `codex/milestone-0-slice-3`. Draft PR [#2](https://github.com/jewilhel/Thinking-Canvas/pull/2) carries the completed slice through protected-main review. GitHub Actions run `31513150545` passed the complete `quality` job, and immutable Netlify deploy preview `6a7b4f2d181b460008f85cf0` built commit `11ba72d`. The additive migration is applied to the approved `thinking-canvas-preview` Supabase project. Netlify branch deploy `6a7b4a98097a4266cdcb3618` exposes the private Slice 3 harness without changing production configuration. The first hosted Realtime connection briefly encountered Supabase's managed `MissingPartition` response while its `realtime.messages` partitions initialized. A clean retry succeeded after the managed partitions appeared; commit `1065cba` additionally ensures any failed subscription is removed before reconnect, preventing a stale-channel collision.

The remote walkthrough used two independent live tabs and Realtime clients under the approved synthetic owner session because Netlify team protection correctly blocked a second browser profile that was not a Netlify project member. Both clients subscribed with two Presence entries, concurrently appended sequences 1 and 2, and converged on the same hash. One client disconnected at object count 2 while the other appended sequence 3; reconnect restored object count 3, sequence 3, and the matching hash. Snapshot v1 then pruned exactly three covered updates. A fresh reload restored all three objects from the snapshot with the same hash, and immediate recompaction pruned zero. The local automated scenario separately proves distinct owner/editor browser contexts and role enforcement.

Slice 4 began on `codex/milestone-0-slice-4` from merged Slice 3 commit `a11a15f`. It adds a deterministic 1,000-object canonical fixture, renderer-independent viewport and connector geometry, and a browser-only Konva spike loaded across an explicit client boundary. Konva remains a derived view: selection, movement, resizing, pointer-centered zoom, panning, and connector endpoints operate on canonical object geometry rather than serialized scene nodes. The spike mounts 800 mixed content objects and 200 connectors, exposes deterministic evidence controls, and captures frame timing during a representative camera interaction.

The first benchmark updated React state on every animation frame and averaged only 16.5 fps. Moving the transient camera transform into imperative Konva stage updates removed unnecessary reconciliation of all 1,000 nodes while preserving canonical object state. On the approved product-owner Mac in current Chrome, the corrected run averaged 74.1 fps with a 17.6 ms p95, a 33.3 ms longest frame, 89 captured frames, and no sustained period below 30 fps. Visual inspection also exposed that the protected shell had dark styling without the root `.dark` theme selector; enabling it restored readable outline-button contrast across the existing Slice 3 and new Slice 4 controls.

The focused-document spike mounts Lexical only after entering a document and binds its text to a document-owned Yjs text type. A strict document-internal Yjs map stores isolated visual objects without parent-canvas IDs or connector endpoint fields. A small preview-only BroadcastChannel and local-storage provider proves same-origin tab convergence and reload without claiming production document durability; Slice 3 remains the durable Supabase persistence proof. The packaged Lexical collaboration plugin changed editor state without updating the supplied Yjs document in this spike lifecycle, so the evidence harness uses an explicit Lexical-to-`Y.Text` bridge. This proves collaborative plain text inside Lexical, focus entry and exit, parent isolation, convergence, and reload; production rich-format schema and provider integration remain later implementation work.

Commit `77622ba` publishes the implementation and local evidence on `codex/milestone-0-slice-4`. Netlify branch deploy `6a7b5f074c6a30dfa8fd3148` exposes the authenticated Slice 4 harness at its private predictable URL. The hosted Chrome walkthrough repeated selection, movement, resizing, connector endpoint updates, the 1,000-object measurement, two-tab document convergence, internal-object isolation, focus exit and re-entry, and reload restoration. Draft PR [#3](https://github.com/jewilhel/Thinking-Canvas/pull/3) carries the slice through protected-main review; GitHub Actions run `31521003121` passed the complete `quality` job, and immutable Netlify deploy preview `6a7b64a7ddc7b10007e7022c` built commit `bc63322`.

Slice 5 began on `codex/milestone-0-slice-5` from merged Slice 4 commit `aeab4e4`. The Responses spike projects at most 200 canonical objects and 64 KiB of summarized semantic context, treats canvas content as untrusted data, forces one strict `update_canvas_text` function call, reparses the returned JSON with Zod, rejects targets outside the projection, and executes accepted owner/editor mutations through the existing `executeCommand` boundary. OpenAI receives neither Supabase credentials nor direct persistence authority. Commenter denial and projection limits run before any provider request.

The separate Realtime route authenticates the server session, re-checks current canvas membership, hashes the user ID for the safety identifier, and requests the documented `gpt-realtime-2.1` client secret. The provider-default credential must expire within ten minutes before the route will release it. The browser requests microphone access only after an explicit click, posts its SDP directly to OpenAI with that ephemeral value, and stops tracks, the data channel, and the peer connection on disconnect. Unit tests prove the expiry guard and WebRTC handshake without placing a standard API key value in client code.

Per-object AI records retain stable identity, complete before/after images, and affected-field paths. Reversal restores a field only while its current value still matches the AI after-image. The deterministic browser scenario stages an AI text edit, applies a later human position change, and restores the original text while preserving position `620, 280`; a separate unit case returns an explicit non-destructive conflict when a human later changes the same text field.

Local implementation and all non-provider scenarios pass. On the authenticated Netlify branch deploy, the Responses route returned a real request ID and a validated bounded text update. The first hosted Realtime attempts reached OpenAI but `/v1/realtime/client_secrets` rejected the current documented minimal session body with HTTP 400 `invalid_req_data` / `Invalid request body`. Diagnostic attempts showed the same body-level rejection without a requested model or session configuration, so the final code retained only the current official contract. Read-only project inspection then showed that the product owner's active OpenAI project had no API key of its own. After the product owner created a project key and stored it as a secret Netlify variable for Deploy Previews and branch deploys, the existing authenticated branch function issued an ephemeral credential successfully. The product owner completed a two-way Realtime AI conversation in Chrome and ended it cleanly; the standard key remained outside the browser, logs, fixtures, and Git.

Commit `f1bd5c3` adds privacy-safe logging for the provider request ID and parameter metadata that the OpenAI SDK exposes on failures. Focused formatting, lint, strict-type, and Realtime unit checks pass. A replacement branch deploy could not be promoted because Netlify's deploy-store upload returned an internal HTTP 403 and subsequent uploads remained incomplete; deploys `6a7b7d6e608acfe6db6022ea` and `6a7b7e2b9eaf8398ca2ae79b` therefore do not count as preview evidence. Netlify applied the newly saved runtime secret to the already verified branch function, so deploy `6a7b7588193b2848af4aabe7` remains the authenticated artifact for the successful Realtime scenario.

PR [#4](https://github.com/jewilhel/Thinking-Canvas/pull/4) was squash-merged through protected `main` as `b825cb8`. Final GitHub Actions run `31534352424` passed the complete `quality` job for head `05f2a97`, and immutable Netlify deploy preview `6a7b8960b7e2fe0009ee74bf` reached `ready` for that exact commit. Netlify's deploy validation scanned 93 files and reported no secret matches. Production deploy `6a7b8a778e333f0008519870` then reached `ready` for the merge commit without being used as a substitute for preview acceptance.

After product-owner sign-in, the OpenAI Platform showed one active `Default project`, `gpt-realtime-2.1` in its model-control catalog, zero project API keys, and a free-trial credit balance of `$0.00` with an explicit prompt to add payment details before the next API request. This signed-in project therefore cannot currently supply the standard server-side API key required for owned Realtime preview verification. The credential already present in the hosted function is not represented in this project's API-key inventory; it may continue to support the prior Responses evidence, but its ownership and Realtime entitlement cannot be established from this account.

Slice 6 began on `codex/milestone-0-slice-6` from merged Slice 5 commit `b825cb8`. It consolidates the final PR, CI, deploy, benchmark, hosted walkthrough, limitation, and decision evidence; maps every Milestone 0 build item, initial database item, library boundary, technical spike, and exit clause; and records the proposed final architecture without changing master-plan completion boxes. The fresh local verification passed formatting, lint, strict types, 48 unit tests, a production build, all 96 role-policy assertions, all nine collaboration durability assertions, and 12 Chromium end-to-end/accessibility scenarios. The standard local `pnpm db:test` wrapper again hit the documented macOS Docker workspace-mount restriction; both SQL files passed directly against the clean reset local PostgreSQL instance with `ON_ERROR_STOP=1`.

The final authenticated walkthrough used ready protected branch deploy `6a7b84f7f77bd226b8608bbb` at `https://milestone-0-slice-5--thinking-canvas.netlify.app/spikes`. Slice 6 changes only this evidence record, so the deployed application code is the same merged Slice 5 artifact; this preserves an authenticated product check without treating a documentation-only branch as new application behavior. The owner session appended durable sequence 4, disconnected, reloaded to the same hash `a7b7ecc0644524904bff7cf5a6bf89c7413fe18cc1fc91dab4193d1a254fbcd2`, and compacted snapshot v2 through sequence 4 while pruning one covered update. The canvas captured 89 frames at 73.1 fps average, 17.4 ms p95, and 24.1 ms longest frame with no sustained period below 30 fps. Focused-document editing retained 35 text characters and one internal visual while reporting excluded parent connector fields; exiting returned to the parent canvas. The authenticated Responses request `req_1447ca815ad347cc8475df35a6fdb600` returned a validated text update, and field-aware reversal restored the original text while preserving the later human position `620, 280`. Realtime remained `DISCONNECTED` in this final pass because the product owner's completed two-way conversation and clean disconnect remain the authoritative microphone-permission acceptance evidence.

Two attempted Slice 6 documentation-branch deploys, `6a7b8caea1b303c95a8c03ed` and `6a7b8cf0366dd24dbacd6199`, built successfully but remained in Netlify's `uploading` state while uploading the server function. They do not count as evidence and did not change production configuration or data.

## Verification evidence

| Date       | Environment                                    | Command or scenario                                                                                                                        | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                      | Requirements covered                                                |
| ---------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 2026-08-10 | Local repository                               | Repository inventory and Git status before planning                                                                                        | Pass: private GitHub repository on `main`; no application, migrations, tests, deployment configuration, or earlier milestone documents found; worktree was clean before this document was added. External service configuration remains unverified.                                                                                                                                                                                                                                | Planning inspection                                                           | Milestone selection and baseline only                               |
| 2026-08-10 | GitHub API                                     | Read `main` branch protection for `jewilhel/Thinking-Canvas`                                                                               | Blocked: HTTP 403 states GitHub Pro is required for protection on this private repository, or the repository must be public.                                                                                                                                                                                                                                                                                                                                                       | CLI response captured during planning                                         | Milestone 0 protected-`main` build item                             |
| 2026-08-10 | GitHub API                                     | Verify repository visibility and read `main` protection after the product-owner decision                                                   | Pass: repository visibility is `PUBLIC`; `main` enforces pull requests, strict required `quality` checks, stale-review dismissal, and administrator enforcement.                                                                                                                                                                                                                                                                                                                   | GitHub CLI readback                                                           | Milestone 0 protected-`main` build item                             |
| 2026-08-10 | Local repository                               | `pnpm check`                                                                                                                               | Pass: Prettier, ESLint with zero warnings, strict TypeScript, four Vitest tests, and the Next.js production build completed successfully.                                                                                                                                                                                                                                                                                                                                          | Local command output                                                          | Slice 1 project shell and quality gates                             |
| 2026-08-10 | Local Chromium                                 | `pnpm test:e2e`                                                                                                                            | Pass: the foundation shell rendered and axe reported zero detectable accessibility violations.                                                                                                                                                                                                                                                                                                                                                                                     | Playwright command output                                                     | Slice 1 end-to-end and accessibility                                |
| 2026-08-10 | Local repository                               | `pnpm licenses list --prod --json`                                                                                                         | Pass for initialization inventory: ten declared license identifiers recorded; the LGPL transitive `sharp-libvips` binary is retained for the required production-release review.                                                                                                                                                                                                                                                                                                   | pnpm license inventory                                                        | Slice 1 dependency review                                           |
| 2026-08-10 | Netlify API                                    | Link local checkout and configure `thinking-canvas` for `jewilhel/Thinking-Canvas`                                                         | Partial: site ID `f9d33c05-2c29-4b38-8b39-d7e1c39bee72` is linked and repository build settings target `main`; deployment and context evidence require the foundation commit on GitHub.                                                                                                                                                                                                                                                                                            | Netlify CLI/API readback                                                      | Slice 1 delivery setup                                              |
| 2026-08-10 | GitHub Actions                                 | CI run `31456156129` for commit `f8c521e`                                                                                                  | Pass: the `quality` job completed formatting, lint, strict type checking, four unit tests, production build, Playwright end-to-end testing, and axe accessibility testing.                                                                                                                                                                                                                                                                                                         | GitHub Actions run                                                            | Slice 1 CI and required-test gate                                   |
| 2026-08-10 | Netlify production                             | Deploy `6a7a99edb39a9f0008f58f0b` for commit `f8c521e`                                                                                     | Pass: adapter-backed production deploy reached `ready`; the signed-in browser loaded `/` with title `Thinking Canvas` and the expected Milestone 0 foundation regions.                                                                                                                                                                                                                                                                                                             | Netlify API and browser walkthrough                                           | Slice 1 production deployment                                       |
| 2026-08-10 | Netlify deploy preview                         | Deploy `6a7a9a528ecabd1970640145`                                                                                                          | Pass: the distinct `deploy-preview` context reached `ready`; the signed-in browser loaded its permalink with title `Thinking Canvas` and the same expected foundation regions.                                                                                                                                                                                                                                                                                                     | Netlify API and browser walkthrough                                           | Slice 1 preview deployment                                          |
| 2026-08-10 | Local PostgreSQL                               | Clean application-schema migration and deterministic synthetic seed replay                                                                 | Pass: the forward migration recreated all 17 user-owned tables, constraints, indexes, triggers, grants, and RLS policies; the synthetic five-role fixture loaded successfully.                                                                                                                                                                                                                                                                                                     | Direct local PostgreSQL output                                                | Slice 2 schema, RLS, and seed                                       |
| 2026-08-10 | Local PostgreSQL                               | `rls_policy_matrix.test.sql`                                                                                                               | Pass: all 96 pgTAP assertions passed across every user-owned table and the owner, editor, commenter, viewer, non-member, former-member, and unauthenticated cases.                                                                                                                                                                                                                                                                                                                 | Direct local PostgreSQL output                                                | Slice 2 role-policy matrix                                          |
| 2026-08-10 | Local repository                               | `pnpm check` after Slice 2 schema and command work                                                                                         | Pass: Prettier, ESLint with zero warnings, strict TypeScript, 20 Vitest tests, and the Next.js production build completed successfully.                                                                                                                                                                                                                                                                                                                                            | Local command output                                                          | Slice 2 schemas and command boundary                                |
| 2026-08-10 | Local Chromium                                 | `pnpm test:e2e` after Slice 2 route work                                                                                                   | Pass for the unauthenticated foundation smoke only: one Chromium test passed with zero detectable axe violations. Auth flows remain unverified and open.                                                                                                                                                                                                                                                                                                                           | Playwright command output                                                     | Regression evidence; not Auth acceptance                            |
| 2026-08-10 | Local Supabase                                 | `pnpm db:reset` after repairing deterministic Auth seed fields                                                                             | Pass: the CLI recreated the database, applied the initial migration, loaded the five synthetic Auth identities and application fixtures, and restarted required services.                                                                                                                                                                                                                                                                                                          | Supabase CLI output                                                           | Slice 2 local development and seed                                  |
| 2026-08-10 | Local Chromium                                 | Serial Supabase Auth boundary suite                                                                                                        | Pass: three tests covered the foundation shell plus unauthenticated redirect, password sign-in, server-readable identity, `/app` and `/spikes`, sign-out, post-sign-out denial, and expired unrefreshable-session denial.                                                                                                                                                                                                                                                          | Playwright command output                                                     | Slice 2 Auth and protected routes                                   |
| 2026-08-10 | Local repository                               | `pnpm check` after Auth integration coverage                                                                                               | Pass: Prettier, ESLint with zero warnings, strict TypeScript, 20 Vitest tests, and the Next.js production build completed successfully.                                                                                                                                                                                                                                                                                                                                            | Local command output                                                          | Slice 2 regression gate                                             |
| 2026-08-10 | Local PostgreSQL                               | Direct rerun of `rls_policy_matrix.test.sql` after Auth seed repair                                                                        | Pass: all 96 pgTAP assertions passed. The Supabase `pg_prove` wrapper could not mount the macOS workspace path, so this is direct local database evidence rather than a passing `pnpm db:test` claim.                                                                                                                                                                                                                                                                              | Direct local PostgreSQL output                                                | Slice 2 role-policy regression                                      |
| 2026-08-10 | Supabase preview                               | Link project `ffkwgtxboqievjnizfjg`; `supabase db push --linked --include-seed`                                                            | Pass: the initial migration and deterministic five-identity synthetic seed were applied to the healthy free preview project in `us-west-1`. No production data or production project was used.                                                                                                                                                                                                                                                                                     | Supabase CLI and dashboard readback                                           | Slice 2 remote schema and Auth fixtures                             |
| 2026-08-10 | Supabase preview                               | Authenticated and anonymous Data API visibility smoke                                                                                      | Pass: the synthetic owner read the one seeded canvas, the authenticated non-member read zero canvases, and the unauthenticated client was denied rather than receiving rows.                                                                                                                                                                                                                                                                                                       | Supabase client readback                                                      | Slice 2 remote RLS smoke                                            |
| 2026-08-10 | GitHub Actions                                 | CI run `31461417634` on draft PR #1                                                                                                        | Pass: `quality` completed formatting, lint, strict types, 20 unit tests, local Supabase startup, the 96-assertion database/RLS suite, production build, three Chromium/Auth/accessibility tests, and cleanup.                                                                                                                                                                                                                                                                      | GitHub Actions run                                                            | Slice 2 integration gate                                            |
| 2026-08-10 | Netlify deploy preview                         | Deploy `6a7ab18aa59f8a000828b2b6` for commit `57c331d`                                                                                     | Pass for Slice 2 Auth: unauthenticated `/app` redirected to sign-in; the synthetic owner signed in, reached `/app` and `/spikes` with server-rendered identity, signed out, and was denied protected access afterward.                                                                                                                                                                                                                                                             | Netlify API and signed-in browser                                             | Slice 2 authenticated preview                                       |
| 2026-08-11 | Local Supabase/PostgreSQL                      | Clean migration replay and direct execution of both pgTAP files                                                                            | Pass: the additive collaboration migration applied after the initial schema and seed; all 105 assertions passed, including server-assigned sequencing, role denial, verified pruning, retry idempotency, post-compaction sequencing, digest rejection, and Realtime policy installation.                                                                                                                                                                                           | Supabase reset and direct PostgreSQL output                                   | Slice 3 persistence, compaction, and authorization                  |
| 2026-08-11 | Local repository                               | `pnpm check` after Slice 3 implementation                                                                                                  | Pass: formatting, ESLint with zero warnings, strict TypeScript, 24 Vitest tests, and the Next.js production build completed successfully.                                                                                                                                                                                                                                                                                                                                          | Local command output                                                          | Slice 3 state, overlap-load, and compaction regression gate         |
| 2026-08-11 | Local Chromium and Supabase Realtime           | Complete four-test Playwright suite, including the two-browser collaboration scenario                                                      | Pass: owner and editor simultaneously appended updates and converged; the disconnected editor caught up durably; a fresh viewer loaded the compacted snapshot; Presence reflected authorized clients; verified compaction pruned three covered updates and an immediate retry pruned zero.                                                                                                                                                                                         | Playwright output and deterministic UI hashes                                 | Slice 3 local collaboration, reconnect, persistence, and compaction |
| 2026-08-11 | Netlify branch deploy and Supabase preview     | Deploy `6a7b4a98097a4266cdcb3618`; run two live clients, disconnect/reconnect, compaction, fresh reload, and retry                         | Pass: two private-channel clients showed two Presence entries, concurrently reached sequence 2 and one hash, diverged 3/2 during disconnect, reconverged at sequence 3 and hash `9c54cefa…`, published snapshot v1 while pruning three updates, restored the same state on fresh reload, and pruned zero on retry. No browser warnings or errors remained.                                                                                                                         | Netlify API, signed-in browser walkthrough, and captured final evidence state | Slice 3 authenticated preview acceptance                            |
| 2026-08-11 | GitHub Actions                                 | CI run `31513150545` on draft PR #2                                                                                                        | Pass: the protected `quality` job completed formatting, lint, strict types, 24 unit tests, local Supabase startup, all 105 database/RLS assertions, production build, four Chromium collaboration/Auth/accessibility tests, and cleanup.                                                                                                                                                                                                                                           | GitHub Actions run                                                            | Slice 3 integration gate                                            |
| 2026-08-11 | Netlify deploy preview                         | Deploy `6a7b4f2d181b460008f85cf0` for commit `11ba72d`                                                                                     | Pass: the immutable PR #2 `deploy-preview` context reached `ready` for the exact CI-tested head without changing production environment configuration.                                                                                                                                                                                                                                                                                                                             | Netlify API readback                                                          | Slice 3 immutable preview artifact                                  |
| 2026-08-11 | Local repository                               | `pnpm check` after Slice 4 implementation                                                                                                  | Pass: Prettier, ESLint with zero warnings, strict TypeScript, 31 Vitest tests, and the Next.js production build completed successfully.                                                                                                                                                                                                                                                                                                                                            | Local command output                                                          | Slice 4 geometry, fixture, timing, and document-model regression    |
| 2026-08-11 | Local Chromium and Supabase Auth               | Complete six-test Playwright suite, including canvas interaction and two-tab focused-document scenarios                                    | Pass: the 1,000-object fixture zoomed, panned, selected, moved, resized, and retained connector anchoring; two tabs converged on Lexical text and isolated internal visuals; exit, re-entry, and reload restored the document state. Existing Auth, collaboration durability, and axe scenarios also passed.                                                                                                                                                                       | Playwright output and deterministic evidence controls                         | Slice 4 interaction, isolation, convergence, and reload             |
| 2026-08-11 | Product-owner Mac and current Chrome           | Capture 90-frame Konva camera-interaction benchmark with 1,000 visible mixed objects                                                       | Pass: 89 measured frames averaged 74.1 fps; p95 was 17.6 ms, the longest frame was 33.3 ms, and no sustained period fell below 30 fps. The canvas remained directly manipulable and Chrome reported no warnings or errors.                                                                                                                                                                                                                                                         | Signed-in Chrome benchmark and rendered metrics                               | Slice 4 approved performance target                                 |
| 2026-08-11 | Netlify branch deploy and current Chrome       | Deploy `6a7b5f074c6a30dfa8fd3148`; repeat canvas transformations, measurement, two-tab document convergence, focus transitions, and reload | Pass: the private authenticated preview rendered 1,000 objects at 72.7 fps average with an 18.1 ms p95, a 25 ms longest frame, and no sustained period below 30 fps. Moving the selected shape changed its anchored connector endpoint. Two tabs converged on Lexical text and one isolated internal visual; exit, re-entry, and reload restored both while parent connector fields remained excluded.                                                                             | Netlify API and signed-in Chrome walkthrough                                  | Slice 4 authenticated preview acceptance                            |
| 2026-08-11 | GitHub Actions                                 | CI run `31521003121` on draft PR #3                                                                                                        | Pass: the protected `quality` job completed formatting, lint, strict types, 31 unit tests, local Supabase startup, all 105 database/RLS assertions, production build, six Chromium collaboration/Auth/canvas/document/accessibility tests, and cleanup.                                                                                                                                                                                                                            | GitHub Actions run                                                            | Slice 4 integration gate                                            |
| 2026-08-11 | Netlify deploy preview                         | Deploy `6a7b64a7ddc7b10007e7022c` for commit `bc63322`                                                                                     | Pass: the immutable PR #3 `deploy-preview` context reached `ready` for the exact CI-tested head without changing production environment configuration.                                                                                                                                                                                                                                                                                                                             | Netlify API readback                                                          | Slice 4 immutable preview artifact                                  |
| 2026-08-11 | Local repository                               | `pnpm check` after Slice 5 implementation                                                                                                  | Pass: Prettier, ESLint with zero warnings, strict TypeScript, 48 Vitest tests, and the Next.js production build completed successfully. The build exposes only server routes for standard OpenAI API-key use.                                                                                                                                                                                                                                                                      | Local command output                                                          | Slice 5 projection, validation, WebRTC, and reversal units          |
| 2026-08-11 | Local Chromium and Supabase Auth               | Complete 12-test Playwright suite, including six AI, Realtime authorization, configuration, and reversal scenarios                         | Pass: unauthenticated routes returned 401; commenters were denied AI mutation before provider access; non-members were denied Realtime credentials; a 201-object projection returned 413 before provider access; missing provider configuration stayed explicit and non-secret; field-aware reversal restored AI text while preserving the later human position. Existing Auth, collaboration, canvas, document, and axe scenarios also passed.                                    | Playwright output and deterministic evidence controls                         | Slice 5 local authorization, limits, reversal, and regression       |
| 2026-08-11 | Local configuration inventory                  | Inspect shell and linked Netlify environment names for `OPENAI_API_KEY`                                                                    | Inconclusive by design: no key exists in the local shell, and Netlify's environment listing did not expose the sensitive value. The subsequent authenticated Responses call proves that the linked preview has a working server-side key. No secret value was printed or committed.                                                                                                                                                                                                | Environment-name inventory plus hosted provider response                      | Slice 5 provider configuration boundary                             |
| 2026-08-11 | Netlify branch deploy and current Chrome       | Deploy `6a7b74467cf08900ea6718cc`; run one bounded Responses mutation, field-aware reversal, and Realtime credential attempt               | Partial: the authenticated Responses route returned request `req_bc4faba0dff940f6891e9691bcec9fa2` and a validated text-only update. Reversal restored the original text while preserving position `620, 280`. The standard key remained server-only. Realtime client-secret creation reached OpenAI but returned HTTP 400 `invalid_req_data` even for the provider-default diagnostic body, so the live WebRTC item remains open.                                                 | Netlify API, signed-in browser, and privacy-safe function log                 | Slice 5 hosted Responses and reversal pass; live voice blocked      |
| 2026-08-11 | Local repository and Netlify deploy API        | Add privacy-safe Realtime request metadata, run focused checks, and attempt a replacement branch deploy                                    | Partial: Prettier, ESLint, strict TypeScript, and four focused Realtime tests pass. Netlify's deploy-store upload returned an internal HTTP 403 and replacement deploys remained incomplete, so the improved provider request-ID diagnostic is not represented as hosted evidence.                                                                                                                                                                                                 | Commit `f1bd5c3`, local command output, and Netlify deploy API                | Slice 5 provider diagnosis; preview evidence still open             |
| 2026-08-11 | OpenAI Platform project settings               | Inspect the signed-in project's billing state, API-key inventory, and model controls                                                       | Blocked: the active `Default project` lists `gpt-realtime-2.1` in model controls but has zero API keys and `$0.00` free-trial credit. The dashboard requires payment details before the next API request, so this project cannot yet provide an owned standard key for the Realtime preview test.                                                                                                                                                                                  | Signed-in read-only OpenAI Platform inspection                                | Slice 5 provider configuration boundary                             |
| 2026-08-11 | Netlify branch deploy and current Chrome       | Save the product-owner project key as a secret preview variable; establish, use, and end an authenticated Realtime WebRTC session          | Pass: Netlify showed `OPENAI_API_KEY` as a secret with two values in two deploy contexts and Functions scope. The authenticated route issued an ephemeral credential, the product owner held a two-way Realtime AI conversation in Chrome, and ending it returned the harness to `DISCONNECTED`. The standard key did not enter the browser, logs, fixtures, or Git.                                                                                                               | Netlify variable metadata, product-owner conversation, and browser readback   | Slice 5 live voice preview acceptance                               |
| 2026-08-11 | GitHub Actions                                 | Final CI run `31534352424` on PR #4 head `05f2a97`                                                                                         | Pass: the protected `quality` job completed formatting, lint, strict types, 48 unit tests, local Supabase startup, all 105 database/RLS assertions, production build, 12 Chromium Auth/collaboration/canvas/document/AI/Realtime/reversal/accessibility tests, and cleanup.                                                                                                                                                                                                        | GitHub Actions run                                                            | Slice 5 integration gate                                            |
| 2026-08-11 | Netlify deploy preview                         | Deploy `6a7b8960b7e2fe0009ee74bf` for commit `05f2a97`                                                                                     | Pass: the immutable PR #4 `deploy-preview` context reached `ready` for the exact final CI-tested head. Netlify scanned 93 files and reported no secret matches.                                                                                                                                                                                                                                                                                                                    | Netlify API readback                                                          | Slice 5 immutable preview artifact and secret-scan gate             |
| 2026-08-11 | Netlify production                             | Deploy `6a7b8a778e333f0008519870` for merge commit `b825cb8`                                                                               | Pass: the merged Slice 5 commit reached `ready` with the Next.js adapter. This confirms delivery only; Slice 5 acceptance remains based on the authenticated non-production preview.                                                                                                                                                                                                                                                                                               | Netlify API readback                                                          | Post-merge delivery confirmation                                    |
| 2026-08-11 | Local repository, PostgreSQL, and Chromium     | Run the complete Slice 6 local quality, clean-reset database/RLS, and browser suite                                                        | Pass: Prettier, ESLint, strict TypeScript, 48 Vitest tests, production build, 96 role-policy assertions, nine collaboration durability assertions, and 12 Chromium Auth/collaboration/canvas/document/AI/Realtime/reversal/accessibility scenarios completed. The Supabase wrapper repeated the documented macOS mount failure; direct clean-database SQL execution passed with stop-on-error enabled.                                                                             | Local command output                                                          | Slice 6 full local regression and evidence consolidation            |
| 2026-08-11 | Authenticated Netlify branch deploy and Chrome | Final consolidated walkthrough on deploy `6a7b84f7f77bd226b8608bbb`                                                                        | Pass: durable sequence 4 survived disconnect/reload with the same hash; verified snapshot v2 pruned one covered update; 1,000-object canvas measured 73.1 fps average and 17.4 ms p95; focused document retained text plus one isolated internal visual; authenticated Responses returned a validated update; field-aware reversal restored text while preserving position `620, 280`. The earlier product-owner Realtime conversation remains the microphone acceptance evidence. | Signed-in browser walkthrough and existing Slice 5 voice evidence             | Slice 6 authenticated final-review scenario                         |

## Change record

| Date       | Change or decision                                                                                 | Rationale                                                                                                                                                                                                                  | Impact                                                                                                                                                                                    | Approved by                   |
| ---------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| 2026-08-10 | Created the first detailed milestone plan for Milestone 0.                                         | No milestone implementation has begun, so the master ledger and skill workflow identify Milestone 0 as the required starting point.                                                                                        | Defines proposed scope and approval gates; does not authorize implementation until approved.                                                                                              | Pending                       |
| 2026-08-10 | Use a public GitHub repository.                                                                    | Public visibility makes branch protection available on the current GitHub plan while preserving the master-plan requirement.                                                                                               | The repository is now public; protected `main` can be configured during Slice 1 after required CI check names are established.                                                            | Product owner                 |
| 2026-08-10 | Approved the Milestone 0 plan, OpenAI Realtime architecture, and spike performance baseline.       | The product owner approved all required decisions after reviewing the milestone plan.                                                                                                                                      | Status changed to `Approved for implementation`; Slices 1–6 are authorized, while final architecture approval remains the evidence-based closure decision.                                | Product owner                 |
| 2026-08-10 | Made the Netlify Next.js adapter explicit after the first ready deploy returned 404.               | The first artifact contained raw `.next` output and no adapter-created routing or function layer.                                                                                                                          | Commit `f8c521e` produced verified production and deploy-preview shells; the project remains private and no public launch occurred.                                                       | Engineering                   |
| 2026-08-10 | Began Slice 2 on a protected-main feature branch and implemented its database/schema/command core. | These boundaries must exist before collaboration persistence can safely begin.                                                                                                                                             | Four of six Slice 2 checklist items now have local automated evidence; Auth session and authenticated-preview evidence remain open.                                                       | Engineering                   |
| 2026-08-10 | Repaired the synthetic Auth seed and completed local server-session acceptance.                    | GoTrue cannot scan a null `email_change_token_new` during password login; the prior database-only seed evidence did not exercise that path.                                                                                | All six Slice 2 local checklist items now pass. CI exports only the local Supabase public URL/key for the same Auth E2E flow; authenticated Netlify preview evidence remains separate.    | Engineering                   |
| 2026-08-10 | Created the free non-production Supabase preview project and opened draft PR #1.                   | Slice 2 required real remote Auth and deploy-preview evidence before collaboration work could safely depend on its boundaries.                                                                                             | Migration, synthetic seed, CI, and the deployed Auth walkthrough pass. The project remains private and no production configuration or launch was changed.                                 | Product owner and engineering |
| 2026-08-11 | Began Slice 3 on a fresh branch from the merged Slice 2 main commit.                               | The approved next vertical slice must prove collaboration durability before canvas or document feasibility work begins.                                                                                                    | Five Slice 3 items now have local automated evidence. The remote migration, CI/branch preview, and signed-in two-browser preview scenario remain before the slice can be marked complete. | Product owner and engineering |
| 2026-08-11 | Completed the Slice 3 collaboration durability spike and opened draft PR #2.                       | Local automation and the authenticated remote walkthrough now cover convergence, overlap-safe persistence, disconnect recovery, verified pruning, reload, and retry behavior.                                              | All six Slice 3 checklist items are complete and the protected-main quality gate passed. Milestone 0 remains open for Slices 4 through 6 and final architecture approval.                 | Product owner and engineering |
| 2026-08-11 | Began Slice 4 and implemented the canvas and focused-document feasibility harness.                 | Canvas rendering cost and document isolation must be proven before committing later milestones to these boundaries.                                                                                                        | Local automation and the approved Chrome benchmark pass. Authenticated Netlify preview evidence remains before Slice 4 can be marked complete.                                            | Product owner and engineering |
| 2026-08-11 | Completed the Slice 4 canvas and focused-document feasibility spike and opened draft PR #3.        | Local automation, the approved-hardware benchmark, authenticated hosted walkthrough, protected CI, and immutable deploy preview now cover the slice's interaction, performance, isolation, convergence, and reload claims. | All four Slice 4 checklist items and the protected integration gate are complete. Milestone 0 remains open for Slices 5 and 6 and the separate final architecture approval.               | Product owner and engineering |
| 2026-08-11 | Began Slice 5 and completed its AI-command, Realtime-boundary, and reversal implementation.        | AI authority, client credential exposure, and concurrent-edit reversal are the remaining expensive feasibility risks before architecture consolidation.                                                                    | Six of seven Slice 5 items pass locally; hosted Responses and reversal also pass. Live Netlify voice remains open on the provider client-secret HTTP 400 blocker.                         | Product owner and engineering |
| 2026-08-11 | Completed Slice 5 after installing a product-owner OpenAI project key for preview contexts.        | The earlier credential supported Responses but was not represented in the signed-in project's key inventory and failed Realtime client-secret creation.                                                                    | All seven Slice 5 items now pass. Authenticated hosted Responses, ephemeral-credential WebRTC conversation, clean disconnect, and field-aware reversal are verified; Slice 6 is next.     | Product owner and engineering |
| 2026-08-11 | Opened draft PR #4 and recorded the protected CI and immutable deploy-preview evidence.            | Slice 5 requires reproducible integration evidence for the exact reviewed commit before protected-main merge.                                                                                                              | GitHub Actions run `31533888178` passed, Netlify deploy `6a7b88101435a30008df5c3b` reached `ready`, and the deploy secret scan reported no matches.                                       | Product owner and engineering |
| 2026-08-11 | Began Slice 6 evidence consolidation and final architecture review.                                | Slices 1–5 are merged and their implementation risks have direct local, CI, and hosted evidence; Milestone 0 now needs one traceable decision record and final walkthrough.                                                | Every build item, library boundary, database item, technical spike, and exit clause is mapped; the proposed final architecture is recorded without changing master-plan checkboxes.       | Product owner and engineering |
| 2026-08-11 | Completed technical verification for Slice 6 and requested Milestone 0 closure approval.           | The complete local suite and the authenticated final review scenario now reproduce the approved spike evidence on the final application artifact.                                                                          | Milestone 0 is ready for an evidence-based architecture decision; master-plan checkboxes remain unchanged until the product owner explicitly approves closure.                            | Product owner and engineering |
| 2026-08-11 | Approved and closed the final collaboration, persistence, rich-text, AI, and voice architecture.   | The product owner reviewed the consolidated trace, local/CI suites, authenticated preview evidence, limitations, and proposed architecture.                                                                                | Milestone 0 is closed. Its genuinely proven master-plan items are checked; later milestone requirements, decisions, and launch gates remain unchanged.                                    | Product owner                 |

## Closure

Closure status: Closed
Closure approval: Product owner approved 2026-08-11
Closed on: 2026-08-11
